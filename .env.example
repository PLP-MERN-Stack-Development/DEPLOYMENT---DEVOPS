# ----------------------------
# SERVER ENVIRONMENT VARIABLES
# ----------------------------

# Default environment (development or production)
NODE_ENV=development

# Server port
PORT=5000

# Development MongoDB URI
DEV_MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/bugtracker-dev?retryWrites=true&w=majority&appName=<app-name>

# Production MongoDB URI
PROD_MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/bugtracker-prod?retryWrites=true&w=majority&appName=<app-name>

# Sentry DSN (for error tracking)
SENTRY_DSN=https://<public-key>@<sentry-host>/<project-id>


# ----------------------------
# CLIENT ENVIRONMENT VARIABLES
# ----------------------------

# Base API URL for development (usually localhost)
VITE_API_URL=http://localhost:5000/api

# Base API URL for production (Render deployed backend)
# Note: You can switch this manually before building for production
# OR use environment-specific .env files (e.g., .env.production)
# Example: https://trackabug.onrender.com/api
# Uncomment and set as needed:
# VITE_API_URL=https://trackabug.onrender.com/api
